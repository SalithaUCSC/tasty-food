const Footer = () => {
    return (
        <footer className="footer mt-auto py-5 bg-dark fixed-bottom">
            <div className="container">
                <span className="text-muted">Footer</span>
            </div>
        </footer>
      );
}

export default Footer;